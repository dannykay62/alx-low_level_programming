#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: argument counter
 * @argv: argument vector
 * Return: always return 0 success
 */

int main(int argc, char *argv)
{
	if (argv[0])
		printf("%d\n", counter);
	return (0);
}
